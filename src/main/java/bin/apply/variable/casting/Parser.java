package bin.apply.variable.casting;

import bin.apply.Repository;
import bin.apply.repository.AccessList;

public class Parser implements Repository {
    public void parser(String line) {
        // ㅇㅁㅇ 변수명:값
        
    }
}
